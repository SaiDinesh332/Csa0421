#include <stdio.h>

int main() {
    int indexBlock[10], n;

    printf("Enter number of blocks: ");
    scanf("%d", &n);

    printf("Enter block numbers:\n");
    for (int i = 0; i < n; i++)
        scanf("%d", &indexBlock[i]);

    printf("Indexed File Allocation:\n");
    for (int i = 0; i < n; i++)
        printf("Index %d -> Disk Block %d\n", i, indexBlock[i]);

    return 0;
}


OUTPUT:
Enter number of blocks: 4
Enter block numbers:
5 12 9 20
Indexed File Allocation:
Index 0 -> Disk Block 5
Index 1 -> Disk Block 12
Index 2 -> Disk Block 9
Index 3 -> Disk Block 20
